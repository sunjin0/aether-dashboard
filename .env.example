# Dashboard 对外暴露的宿主机端口；默认 8001。
DASHBOARD_PORT=8001

# Admin 后端在 Docker 共享网络中的访问地址；默认使用 aether-admin 容器名和 8080 端口。
AETHER_ADMIN_UPSTREAM=http://aether-admin:8080

# Docker 共享网络名称；需与 Admin、MCP、Deep Agent 所在网络一致。
AETHER_SHARED_NETWORK=aether-mcp-server_default
